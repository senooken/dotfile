// (File name: )
// Author: SENOO, Ken
// (Last Update: )
// Lincense: MIT

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <string>
#include <boost/algorithm/string.hpp> // boost::split(in, str, delim), trim/trim[copy]_if(str, delim)
#include <boost/lexical_cast.hpp> // boost::lexical_cast<type>(obj)

int main() {
  
  return 0;
}
